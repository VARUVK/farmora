## Packages
recharts | For data visualization of market price trends
framer-motion | For smooth animations and transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
date-fns | Date formatting and manipulation
lucide-react | Icon library (already in base but ensuring version match)
@radix-ui/react-slot | For component composition
@radix-ui/react-dialog | For modal dialogs
@radix-ui/react-label | For form labels
@radix-ui/react-select | For dropdown selection
@radix-ui/react-scroll-area | For scrolling content areas
@radix-ui/react-avatar | For user avatars
@radix-ui/react-separator | For visual dividers
@radix-ui/react-tooltip | For tooltips

## Notes
- Theme: Agricultural (Greens, Browns, Earth tones).
- Authentication: Replit Auth (use-auth.ts hook).
- Chat Integration: Uses generic AI chat endpoint, needs context-aware UI.
- Charts: Mock historical data for now, use recharts for visualization.
- Multi-language: Use a simple React Context for managing language state (EN/TA/HI).
